/* if(argsArray[0] === 'promote'){
        promote(argsArray[1]);
        return
        }
        if(argsArray[0] === 'demote'){
            demote(argsArray[1])
            return
        }
        if(argsArray[0] === 'get'){
            getUserPermissions(argsArray[1])
        } */
let a;
function manue() {
    a = '123'
}
manue('CAROTA')
console.log(a)